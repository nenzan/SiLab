<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.compunerd.silab">

    <application
        android:allowBackup="true"
        android:icon="@drawable/surveyor_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.ProfilActivity" />
        <activity android:name=".view.OrderActivity" />
        <activity android:name=".view.KeranjangActivity" />
        <activity android:name=".view.KontakActivity"></activity>
    </application>

</manifest>